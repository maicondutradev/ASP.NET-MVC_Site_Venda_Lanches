@model IEnumerable<LanchesSaq.Models.Lanche>

<div>
    <h2>Todos os lanches</h2>
</div>

@foreach(var lanche in Model)
{
    <div>
        <h4>@lanche.Nome</h4>
        <p><img src="@lanche.ImagemUrl" /></p>
        <h3>@lanche.Preco.ToString("c")</h3>
    </div>
}